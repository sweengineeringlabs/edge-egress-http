//! Default HTTP breaker impl contract — counterpart for `core::default_http_breaker`.
//!
//! The concrete implementation struct lives in `core::default_http_breaker`;
//! it implements [`HttpBreaker`](crate::api::http_breaker::HttpBreaker).
