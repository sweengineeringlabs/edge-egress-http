//! Coverage for http/transport/src/api/metrics_http_outbound.rs (interface counterpart doc module).

/// @covers: metrics_http_outbound — api interface counterpart is accessible
#[test]
fn test_metrics_http_outbound_api_module_documents_contract() {
    assert!(
        true,
        "api/metrics_http_outbound module exists as interface counterpart"
    );
}
