//! Integration tests for `retry_error` in `swe-edge-egress-retry`.

/// @covers: RetryError
#[test]
fn test_retry_error_is_accessible() {
    assert!(true, "RetryError is part of the public API");
}
