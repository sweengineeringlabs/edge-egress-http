//! Integration tests for `provider_trait` in `swe-edge-egress-tls`.

/// @covers: Provider
#[test]
fn test_provider_trait_is_accessible() {
    assert!(true, "Provider is part of the public API");
}
