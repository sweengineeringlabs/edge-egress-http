//! Interface counterpart for `core::auth::middleware::counting_http_auth`.
//!
//! The corresponding core file provides `AuthMiddleware` impls and a test-only
//! `CountingHttpAuth` stub.

pub use crate::api::types::auth::AuthMiddleware;
