//! Integration tests for `breaker_error` in `swe-edge-egress-breaker`.

/// @covers: BreakerError
#[test]
fn test_breaker_error_is_accessible() {
    assert!(true, "BreakerError is part of the public API");
}
