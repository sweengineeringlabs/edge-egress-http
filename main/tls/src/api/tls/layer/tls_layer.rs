//! Interface counterpart for `core::tls::layer::tls_layer`.

/// Interface counterpart for the TLS request-pipeline middleware layer.
#[expect(
    dead_code,
    reason = "SEA api/ type alias anchor — intentionally unused"
)]
pub type TlsLayer = crate::api::types::TlsLayer;
